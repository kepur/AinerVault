[project]
name = "ainern2d-shared"
version = "0.1.0"
description = "Shared contracts, models and infra helpers for Ainer"
requires-python = ">=3.11"
dependencies = [
	"pydantic>=2.9.0",
	"sqlalchemy>=2.0.0",
	"alembic>=1.13.0",
]

