FROM python:3.12-slim

WORKDIR /srv
RUN pip install --no-cache-dir "fastapi>=0.115" "uvicorn[standard]>=0.30" "httpx>=0.27"

COPY apps/ainer-core/mock_gateway /srv/mock_gateway

EXPOSE 8199
CMD ["uvicorn", "mock_gateway.main:app", "--host", "0.0.0.0", "--port", "8199"]
